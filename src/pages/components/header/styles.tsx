import styled from "styled-components";
import { Navbar } from "react-bootstrap";

export const Eyecuenav = styled(Navbar)`
    background: rgba(0,0,0,0);
    @media screen and (max-width: 768px) {
        .navbar-collapse {
             height: 100vh;
        }

     }
`



